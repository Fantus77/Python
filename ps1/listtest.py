"""
This is where I start to mess around with lists
"""

lst = list(range(1, 101))
val = 14
def FindBest(lst, val):
    lstlen = len(lst)
    fun = lst[lstlen]
    print(fun)


FindBest(lst, val)